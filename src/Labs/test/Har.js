import React, { useState } from "react";

function Har() {
    // const [dsa, setDsa] = useState([123, 234, 345, 456]);
    // const qwe = (ert) => {
    //   setDsa([567, ...dsa]);
    // };
    // const wer = (rty) => {
    //   zxc(qwe.filter((x) => x !== rty));
    // };
    return (
      <div>
        {/* <h2>{JSON.stringify(dsa)}</h2> */}
        <h2>Hi</h2>
        {/* <button onClick={() => qwe(ert)}>EWQ</button>
        <button onClick={() => wer(rty)}>REW</button> */}
      </div>
    );
  }


  export default Har()